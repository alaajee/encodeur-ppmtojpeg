#ifndef ZIGZAG_H
#define ZIGZAG_H

#include <stdio.h>  

#define N 8 

// La fonction zigzag réorganise les éléments d'une matrice 8×8 8×8 en suivant un parcours en zig-zag, puis les place dans une nouvelle matrice 8×88×8. 
int zigzag(int input[N][N], int output[N][N]);

#endif /* ZIGZAG_H */
